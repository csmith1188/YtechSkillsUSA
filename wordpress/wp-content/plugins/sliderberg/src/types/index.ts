/**
 * Central export for all type definitions
 * Import from '@/types' or '../types' to access any type
 */

// Common shared types (FocalPoint, MediaObject, ContentPosition, etc.)
export * from './common';

// Slider block types
export * from './slider';

// Slide block types
export * from './slide';

// WordPress type extensions
export * from './wordpress/blocks';
